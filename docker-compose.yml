version: '3.8'

services:
  dmaas-ui:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - API_BASE_URL=http://localhost:3302
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - dmaas-network

networks:
  dmaas-network:
    driver: bridge
